


document.addEventListener("deviceready", function () {
    AppFramework.init(function () {
        // starts here
        this.ready();
    });
}, false);

AppFramework.init(function () {
    // starts here
    this.ready();
});